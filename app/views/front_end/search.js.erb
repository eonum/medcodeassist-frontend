  var parameters = <%= raw @variables %>;
  var codes = parameters["codes"];
  var li_id = parameters["li_id"];

  var codeList = "";
  codes.forEach(function(element) {
    var listElement = "<li><a>"+element["code"]+": "+element["text_de"]+"</a></li>";
    codeList = codeList + listElement;
  });
  if(codeList.length == 0)
    codeList = "<li>Keine Codes gefunden</li>";
  $("#"+li_id+" div.dropdown ul.dropdown-menu").empty().append(codeList);
  $('ul.dropdown-menu li').click(function() {
    console.log('select');
    console.log($(this).text());
    $("#"+li_id+" div.text_field").text($(this).text());
  });
  $("#"+li_id+" div.dropdown").toggleClass("open", true);