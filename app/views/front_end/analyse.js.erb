  var params = <%= raw @variables %>;
  var words = params["words"];
  //var code = params["codes"];

  var text = $("#textArea").html();
  words.forEach(function (item) {
    var textArray = text.match(new RegExp( "(" + preg_quote( item ) + ")" , 'gi'));
    textArray.forEach(function(word){
      var link = "<a class='highlight showWordDetails' contenteditable='false' data-toggle='tooltip' title='click for showWordDetails'>" + word + "</a>"
      text=text.replace(word, link);
    });
  });
  $("#textArea").html(text);

  function preg_quote( str ) {
    // http://kevin.vanzonneveld.net
    // +   original by: booeyOH
    // +   improved by: Ates Goral (http://magnetiq.com)
    // +   improved by: Kevin van Zonneveld (http://kevin.vanzonneveld.net)
    // +   bugfixed by: Onno Marsman
    // *     example 1: preg_quote("$40");
    // *     returns 1: '\$40'
    // *     example 2: preg_quote("*RRRING* Hello?");
    // *     returns 2: '\*RRRING\* Hello\?'
    // *     example 3: preg_quote("\\.+*?[^]$(){}=!<>|:");
    // *     returns 3: '\\\.\+\*\?\[\^\]\$\(\)\{\}\=\!\<\>\|\:'
    return (str+'').replace(/([\\\.\+\*\?\[\^\]\$\(\)\{\}\=\!\<\>\|\:])/g, "\\$1");
  }