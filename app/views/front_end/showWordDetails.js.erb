var parameters = <%= raw @variables %>;
var word = parameters["word"];
var synonyms = parameters["synonyms"];

$("#infoWord").text(word);

if(synonyms){
  $("#synonymsList").empty();
  synonyms.forEach(function (item) {
    $("#synonymsList").append("<li>"+item+"</li>");
  });
}
